import { SmPaginatorPipe } from './sm-paginator.pipe';

describe('SmPaginatorPipe', () => {
  it('create an instance', () => {
    const pipe = new SmPaginatorPipe();
    expect(pipe).toBeTruthy();
  });
});
